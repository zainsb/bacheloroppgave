@model List<Studentparlamentet_28.Models.Valgtyper>

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <script type="text/javascript">
        
        $(function () {
            
            $.ajax({
                url: '/Bruker/HentAntallValg',
                type: 'GET',
                dataType: 'json',
                success: function (antall) {
                    
                },
                error: function (x, y, z) {
                    alert(x + '\n' + y + '\n' + z);
                }
            });

            
            //Henter inn antall stemte av antall brukere
            /*$.ajax({
                url: '/Bruker/HentResultatAntallStemt',
                data: { id: teller },
                type: 'GET',
                dataType: 'json',
                success: function (melding) {
                    $("#idAntStemt").html(melding);
                    teller++;
                },
                error: function (x, y, z) {
                    alert(x + '\n' + y + '\n' + z);
                }
            });*/
        })
    </script>
</head>
<body>
    <div class="col-sm-12" id="box2">
        
        <div id="tableHentResultat">

            <table class="table  table-responsive table-scrollable" id="tableID">
                <thead class="thead-default">
                    <tr>

                        <th>
                            Type valg
                        </th>
                        <th>
                            Antall Stemt
                        </th>
                        <th>

                            Legg til PDF
                        </th>

                        <th></th>
                    </tr>
                </thead>


                <tbody class="table-bordered">
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.valgtype)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.valgtypeid, new { id = "HiddenID"})
                            </td>
                            <td>
                                
                            </td>
                            <td>
                                @Html.ActionLink("Resultat", "Resultat", new { tom = "" }, new { @class = "btn btn-danger" })
                            </td>
                        </tr>
                    }
                </tbody>

            </table>

        </div>

        @Html.ActionLink("Tilbake", "Tilbake")
        @Html.ActionLink("Last ned PDF", "LastNedListe", new { tom = "" }, new { @class = "btn btn-success" })

        <div id="testDiv"></div>
        <div id="testDiv2"></div>
    </div>
</body>
</html>
